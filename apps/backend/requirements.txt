# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database — SQLAlchemy 2.0 async + PostgreSQL driver
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic>=1.13.0

# Data validation (FastAPI schemas)
pydantic==2.10.3

# AI / Agent (used in Phase 3 & 4)
anthropic==0.40.0
langgraph==0.2.60
langchain-anthropic>=0.3.0   # ChatAnthropic — default provider
langchain-openai>=0.2.0      # ChatOpenAI — used for OpenRouter + Groq (OpenAI-compat API)

# Environment config
python-dotenv==1.0.1

# Auth — signed session cookies (password compared as plaintext via hmac)
itsdangerous==2.2.0
